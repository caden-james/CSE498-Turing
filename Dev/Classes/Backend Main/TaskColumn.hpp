#ifndef CSE_TASK_COLUMN_H
#define CSE_TASK_COLUMN_H

// #include "DynamicString.hpp"
// #include "RandomAccessSet.hpp"
// #include "Card.hpp"

// namespace cse {

// class TaskColumn {
// private:
//     DynamicString title;
//     RandomAccessSet<Card> cards;

// public:
//     TaskColumn(const DynamicString& title);

//     void addCard(const Card& card);
//     bool removeCard(const Card& card);
//     size_t getCardCount() const;

//     Card getCardByIndex(size_t index) const;
//     DynamicString getTitle() const;
// };

// }
#endif
